# VirusTotal API key (optional). Leave blank to run without threat-intel enrichment.
VT_API_KEY=

# Risk score thresholds (0-100). Optional overrides — defaults are used if unset.
RISK_THRESHOLD_LOW=25
RISK_THRESHOLD_MEDIUM=50
RISK_THRESHOLD_HIGH=75
