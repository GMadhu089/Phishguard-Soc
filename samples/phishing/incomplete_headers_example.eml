From: unknown@example.test
Subject: (no subject provided)
Content-Type: text/plain; charset="utf-8"

This is a minimal email with most optional headers missing: no To,
no Date, no Message-ID, no Received headers, no Authentication-Results.

It exists to verify the tool degrades gracefully rather than crashing
when a real-world malformed or stripped-down sample is submitted.
